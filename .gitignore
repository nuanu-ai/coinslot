# OS
.DS_Store

# Editors
.idea/
.vscode/
*.swp

# Claude Code — local settings and agent working copies are not committed
.claude/settings.local.json
.claude/worktrees/

# Secrets and environment
.env
.env.*

# Build and dependencies
node_modules/
dist/
coverage/
*.tsbuildinfo

# pnpm keeps a content-addressed store here when a build fetches into the
# repository rather than into the user store. It is a cache, not a source.
.pnpm-store/
