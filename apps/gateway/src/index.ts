/**
 * Гейтвей Coinslot: 402-край, очередь заказов и квитанции. HTTP здесь пока
 * нет — сначала контракты и машина состояний, край приезжает следом
 * (`docs/research/21-pilot-plan.md`, этап 1).
 *
 * Доменная логика живёт в `@coinslot/core` и про фреймворк не знает, поэтому
 * край заменяем без переписывания продукта (ADR-0003, п. 7).
 */

export { type GatewayConfig, loadConfig } from "./config.js";
