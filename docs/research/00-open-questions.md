# Открытые вопросы

Стартовый список для discovery. Вычёркиваем по мере ответов; ответы, ставшие
решениями, уходят в `docs/decisions/`.

## Продукт
- [x] Что такое Coinslot: какую проблему решает и для кого? → `01-hypothesis.md`
- [ ] Каков минимальный полезный результат (MVP) и что сознательно откладываем?
- [x] Есть ли аналоги/конкуренты, от которых отталкиваемся? →
      `02-agentic-storefronts.md` (ниша пуста; ближайшие: Stripe syndication /
      PayPal Store Sync — фиат; Bitrefill/Travala — прецеденты мерчантов в x402)
- [ ] Механика адреса доставки для физических товаров — когда дойдём до них
- [ ] Возвраты: изучить практику работающих продавцов в агентных платежах,
      взять готовый паттерн → `10-design-backlog.md`
- [x] Остатки и гонка за последний экземпляр → источник правды — мерчант
      через quote-hook в момент покупки; журнала остатков у Coinslot нет
      (`14-integration-model.md`, ADR-0002)
- [ ] Споры: минимальная роль Coinslot, максимум у мерчанта →
      `10-design-backlog.md`
- [ ] Процедура снятия карточек из каталогов — задокументировать →
      `10-design-backlog.md`
- [ ] Видимость на пустом рынке: имитация покупок недопустима — нужен ли
      честный способ поддерживать видимость? → `10-design-backlog.md`
- [ ] Продление подписочных товаров: агент купил месячный доступ — что
      через 30 дней? Для подписочных мерчантов вопрос №1 (гейт, роль
      владельца → `20-task-reading-results.md`)
- [ ] Сбои и обязательства нашей стороны: что обещаем мерчанту при
      молчании Coinslot (сейчас асимметрия: про мерчанта шесть страниц,
      про нас ноль)
- [ ] Юр/налоговый контур сделки: кто продавец, какой документ получает
      бухгалтерия мерчанта, отчётность по стейблкоину (владелец назвал
      блокером; связка с регуляторной линией в `06-prior-art-web2.md` §9)
- [ ] Канал общения мерчанта с покупателем в споре: покупатель — программа,
      человек с поддержкой не переписывается — кому мерчант отвечает?
- [ ] Чьи контакты приходят в параметрах покупки (почта агента или
      человека за ним): кому мерчант открывает кабинет и шлёт уведомления
      (гейт №2, владелец)
- [ ] Ранжирование и ценовая конкуренция в каталогах: с кем и по чему
      сравнивает агент (гейт №2)
- [ ] Юр-идентичность в портале: кто «мы» для мерчанта, который отдаёт
      ключ от API (гейт №2; связка с юр/налоговым контуром выше)
- [ ] Молчащее списание никто не переспрашивает: у клиента фасилитатора есть
      verify, settle и список поддерживаемого — способа спросить «что стало с
      этим списанием» нет, а повторный settle запрещает машина. Заказ остаётся
      в `outcome_unknown`, пока платёжный слой не заговорит сам, и разобрать
      такой случай можно только руками — по словам фасилитатора, записанным в
      заказе. Подробности — в комментарии к `#settle` в
      `apps/gateway/src/app/runner.ts`.

## Ограничения
- [ ] Платформа: web / mobile / desktop / CLI / сервис без UI?
- [ ] Один пользователь или многопользовательская система? Нужны ли аккаунты?
- [ ] Где живут данные: локально, своё хранилище, внешние API?
- [ ] Ограничения по бюджету, срокам, хостингу?

## Стек и архитектура
- [ ] Какие языки/технологии предпочтительны или исключены?
- [ ] Что важнее на старте: скорость итераций или задел на масштаб?
- [ ] Какие интеграции обязательны с первого дня?

## Техрешение этапа 1 (закрываем по ходу)
- [x] Имя и форма заголовка ключа → зафиксированы в contracts
      (`MERCHANT_KEY_HEADER`, `merchantKeyHeaderValue`, `merchantKeyFrom` в
      `packages/contracts/src/api.ts`); гейтвей и SDK импортируют один
      источник, drift ловит тест api.test.ts
- [x] Сборка публикуемых пакетов → есть. `tsc` собирает `@coinslot/sdk` и
      `@coinslot/contracts` в `dist` — JS плюс декларации рядом; бандлер не
      заводили, потому что исходники и так импортируют друг друга через
      `.js`-спецификаторы, которые `tsc` переносит как есть. Внутри монорепо
      ничего не поменялось: `exports` по-прежнему указывает на `src`, а на
      сборку его подменяет `publishConfig` в момент упаковки, — правка видна
      без компиляции. `bin` объявлен там же, поэтому `npx coinslot verify`
      стартует. Проверяет это `pnpm outside`: удаляет `dist`, пакует тарболы
      (сборку делает `prepack`, так что путь тот же, что у релиза), ставит их
      npm-ом в каталог вне репозитория и гоняет там импорт и `verify` —
      с заведомо неполной карточкой как контролем. Чего эта проверка не
      показывает: `npm install @coinslot/sdk` из портала по-прежнему нечего
      скачивать — не публиковали, публикацией занимаются changesets
      (ADR-0003 §8), и `access` в их конфиге стоит `restricted`; публичность
      пакетов решает Дмитрий. Не проверено автоматически: типы в чужом проекте
      (гоняли руками — резолвятся, но при `skipLibCheck: false` и `types: []`
      две наши декларации ссылаются на `AbortSignal` из `@types/node`,
      которого в зависимостях SDK нет)
- [x] Работает ли `verify` по ключу, без файлов → нет, и это решение, а не
      недоделка. Такой проверке нечего проверять: карточка попадает в
      `list_merchant_cards` только пройдя `CardSchema` на публикации, а
      `checkCard` гоняет ту же схему, — то есть проверка по построению всегда
      отвечала бы «полно» и учила бы мерчанта верить зелёному, которое ничего
      не значит. Беcключевой запуск при этом полезен сам по себе: он работает
      до того, как у мерчанта появился ключ и что-то опубликовано.
      Пересматриваем, когда появится маршрут пробной публикации: он проверял бы
      то, чего локальная проверка не может, — отказы, которых нет ни в одной
      схеме (`packages/sdk/src/check-card.ts`)
- [ ] Тестовый путь для идемпотентности verify: нет маршрута, поднимающего
      заказ по своей карточке для теста, и нет способа сделать `Order.test`
      истинным без живого платежа — verify v0 честно останавливается на этой
      половине
- [ ] Как SDK читает ответ с незнакомым словом: строгий разбор превращает
      успех с новым результатом в «нечитаемый документ» и отчитывается о сбое.
      Либо SDK становится терпимым к незнакомым информационным значениям, либо
      версия контракта двигается на каждое такое изменение. Решить до первой
      публикации пакета (ADR-0006 п. 3)
- [ ] Какие наши сроки мерчант обязан видеть или называть сам. Сегодня три
      числа ведут себя по-разному без принципа, который бы их разделял:
      `fulfill_deadline_seconds` необязателен и подставляется из настроек
      шлюза, `confirm_deadline_seconds` устроен так же, а синхронный бюджет
      карточкой не выражается вовсе. Мерчанта при этом держат по всем трём, и
      по первому он может оказаться должен возврат по часам, которых не видел
      ни он, ни агент. Сделать одно из трёх обязательным — оставить два
      молчаливых умолчания и одно громкое; это решение уровня ADR, а не правка
      схемы
- [ ] Параллелизм подписки воркера (имя параметра и умолчание) — портал сам
      держит их нерешёнными
- [ ] Отказывает ли Bazaar в листинге из-за POST-пробы. Первый настоящий
      вердикт `pnpm smoke:listing` (28.08.2026, против живого
      `https://coinslot.nuanu.ai`, до этого проверка честно отвечала «вердикта
      нет»): по обеим карточкам GET-проба **принята** и прошла всё
      обязательное — 402, x402 версии 2, заголовок `PAYMENT-REQUIRED`, схема
      `exact`, сеть `eip155:84532`, USDC, сумма, `payTo`, `maxTimeoutSeconds`,
      и целиком разобралось discovery-расширение Bazaar. POST-проба по обеим
      **отклонена**: валидатор шлёт POST с пустым телом, а покупка без тела —
      это 400, потому что открывать заказ нечем (`routes.ts`, `purchase`).
      Так и задумано: GET существует ровно для краулеров, POST — это покупка.
      Чего мы не знаем: считает ли сам листинг обязательным, чтобы платный
      ресурс отвечал 402 на любой метод. Если да — выбор из двух, и оба
      неприятны: либо 402 выдаётся до разбора тела (тогда агента зовут платить
      за запрос, который потом всё равно откажут — ровно то, чего мы избегаем
      на паузе карточки), либо мы объявляем в discovery только GET и живём с
      отклонённой пробой. Мерить на живом листинге, а не рассуждать
