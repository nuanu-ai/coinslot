{
  "name": "@coinslot/core",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "Coinslot domain core — orders, receipts, the state machine; pure logic with no IO",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "devDependencies": {
    "@coinslot/contracts": "workspace:*",
    "vitest": "^4.1.11"
  }
}
